class NetSalaryRate {
    constructor(min, medium, max) {
        this.min = min;
        this.medium = medium;
        this.max = max;
    }
}

export const netSalaryRate = new NetSalaryRate(26640, 90820, 120956);
